<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyperpass.mapper.HpStaffMapper">

    <resultMap id="staffResultMap" type="com.hyperpass.domain.HpStaff">
        <id     property="id"        column="id"/>
        <result property="username"  column="username"/>
        <result property="password"  column="password"/>
        <result property="name"      column="name"/>
        <result property="role"      column="role"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="staffResultMap">
        SELECT id, username, password, name, role, created_at
        FROM hp_staff
        WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.hyperpass.domain.HpStaff"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hp_staff (username, password, name, role)
        VALUES (#{username}, #{password}, #{name}, #{role})
    </insert>

</mapper>
